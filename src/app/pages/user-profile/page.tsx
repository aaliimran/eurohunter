import LogoutButton from "@/components/buttons/signout-button";
import React from "react";

const UserProfile = () => {
  return (
    <div className="h-screen flex justify-center items-center">
      <LogoutButton />
    </div>
  );
};

export default UserProfile;
